schema {
    query: Query
}

type Task {
  id: ID
  title: String
  description: String
}

type User {
  id: ID
  username: String
}

type Query {
  tasks: [Task]
  task(id: ID!): Task
  users: [User]
  loggedUser: User
}
